<!doctype html>

<html lang="en">

<head>
    <meta charset="utf-8">

    <title>jaqreven</title>
    <meta name="description" content="jaqreven's personal website fuck yea">
    <meta name="author" content="jaqreven">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
    <link rel="stylesheet" href="/css/styles.css">

    <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
</head>

<body>
    <%- include('components/header', {
        title: fursona.name,
        subtitle: fursona.bio,
        icon: fursona.icon,
        color: fursona.color
    }) %>

    <% fursona.galleries.forEach(gallery => { %>
        <%- include('components/gallery', {
            gallery: gallery
        }) %>
    <% }) %>

    <%- include('components/footer', {
        color: fursona.color
    }) %>

    <%- include('components/imagemodal') %>
</body>

</html>